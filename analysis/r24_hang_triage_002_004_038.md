# R24: 002/004/038 挂死活体取证 — 三案同一死点族 FUN_14007A224 游标同步缺失

> 2026-08-27 R24 轮 · V-Gate 门3 attach（attach_process 零地址依赖，断点选址不用）
> 工具链: bash + windbg MCP（attach_process/execute/modules/read_memory/end_session/session_status 全程可用，无 SKIP 项）
> 方法参照: analysis/r23_021_live_stack.md / r23_021_deadpoint_shift.md（本案不复用其绝对死点结论，全部以本次 attach 为准）

---

## 0. 构建身份核对（证据绑定）

| 项 | 值 | 判定 |
|---|---|---|
| C:\pectest\DEPLOYED_BUILD.txt | hash=f8e7cbc md5=f9166420**1a69be1e**4b355a613f6db172 time=2026-08-27 00:55:42 | 绑定对象 |
| build\msvc\pecmd_msvc.exe / pecmd_msvc.map / symsnap.txt | 均 2026-08-27 00:55 | 与部署同批 ✓ |
| git HEAD | abc0084（R24 转发修复，提交时间 01:06 晚于部署） | 见下注 |
| map 内 021 转发 | FUN_14001b23c @0x1401e7640 (unimplemented_stubs.obj 转发桩) + PECMD_ExtractTableSegment @0x140122950 (core_b1_remaining.obj 真体) | **部署体已含 R24 转发** ✓（与 00:58 时 021/037 exit=0 同构建一致） |
| src 行号绑定 | `git diff f8e7cbc -- src/lang/core_execline.c` 为空 | **core_execline.c 与部署构建逐字节一致，src 行号证据合法** ✓ |

> 注：f8e7cbc 为构建所据提交（R23f 改名落地），abc0084 为其子提交（把已构建部署的转发补丁落账）。三案取证期间工作树无新增改动（只读纪律：未改源码/未构建/未部署/未 commit）。

挂死复现（每案）：`python harness/runners/run_case.py <case> --exe msvc --timeout 180`（后台），18s 后 tasklist 确认存活并记录 PID → attach → 取证 → end_session → taskkill。

---

## 1. 三案死点一览（死点 ID）

| 案 | 死点 ID（函数 + RVA + 归属） | 捕获 RIP 相位 | 触发行文本 | 输出缓冲形态 | 容量 |
|---|---|---|---|---|---|
| 002_envi_assign | **FUN_14007A224+0x202~0x2a5 环**（RVA 0x1127c2-0x11286d；map:982 `FUN_14007A224 @0x1401125c0` core_execline.obj；src core_execline.c:119；dc:77689；环头 src:168/dc:77749） | ntdll!RtlReAllocateHeap+0x1259 → RtlCopyMemory（重分配拷贝） | `ENVI C=%A%_%B%`（游标停 %A% 的 `%`） | `" C=HelloHelloHello…"` ×34MB | 0x01074800（17.2M 字符） |
| 004_envi_global_scope | 同上函数，相位 **+0x889 名字比较**（RVA 0x122e49；src:281 StrCmpNIW RANDOM%） | KERNELBASE!StrCmpNIW+0x26 | `%A%`（A="inner"） | `" B=innerinnerinner…"` ×33MB | 0x00ff9000（16.7M 字符） |
| 038_sub_param_shadow | 同上函数，相位 **+0x8da**（RVA 0x122e9a；src:285 StrCmpNIW __THIS% 后） | KERNEL32+0x7cc3 ← KERNELBASE!StrCmpNIW+0xdb | `ENVI H=%a%`（末行） | 增长同型 | 0x00fe3000（16.6M 字符） |

**三案调用链完全同构**：
```
FUN_14007A224 ← PECMD_EvalSpecialToken+0x459 (RVA 0x241d9, core_b2e.obj, src core_b2e.c:1440/1736)
  ← PECMD_ProcessScriptBlock+0x1289 (RVA 0x1a27c9)
  ← PECMD_DispatchExpressionBlock+0xe61 ← PECMD_RunScriptText+0xb6d ← srx_ExecuteScriptFile+0x64b
  ← PECMD_RunCommand+0x124d ← PECMD_ProcessScriptBlock+0x1c75
  ← PECMD_DispatchExpressionBlock+0xe61 ← PECMD_RunScriptText+0xb6d ← PECMD_RunStartupScript+0x14cf
  ← PECMD_RunBootScriptInFiber+0x1c ← PECMD_MainW+0x5d7 ← main+0xf0
```
（021 时代的 ExtractTableSegment 扫描环本轮在三案中**均未出现**——021/037 的转发修复与其无关的三案死点各自独立，本次死点族 = ENVI 展开器。）

---

## 2. 002_envi_assign

- 挂死确认：18s 存活，PID **11444**（63 MB）。复现任务 background job 收尾：exit=124，vars.txt/done.txt 双缺。session: `sess-18cf69fa3571daf0-2`（已 end_session）。
- **完整 k 25（主线程 TID0；线程 1-2 = 线程池 ZwWaitForWorkViaWorkerFactory 等待支座，无 TEAM worker）**：
```
000000ea0bf6d298 00007fff b5ee8979 ntdll!RtlCopyMemory+0x1eb
000000ea0bf6d2a0 00007fff b5ee7a15 ntdll!RtlReAllocateHeap+0x1259
000000ea0bf6d420 00007fff b5ee776d ntdll!RtlReAllocateHeap+0x2f5
000000ea0bf6d5b0 00007ff7 1891bbfd ntdll!RtlReAllocateHeap+0x4d
000000ea0bf6d600 00007ff7 1891c078 pecmd_msvc!PECMD_HeapRealloc+0xed
000000ea0bf6d670 00007ff7 1891b00f pecmd_msvc!PECMD_AllocString+0x28
000000ea0bf6d6b0 00007ff7 18922872 pecmd_msvc!PECMD_StrBldGrowWide+0x10f
000000ea0bf6d730 00007ff7 188341d9 pecmd_msvc!FUN_14007A224+0x2b2      ← ln 实证 = FUN_14007A224+0x2b2 (RVA 0x112872)
000000ea0bf6dbd0 00007ff7 189b27c9 pecmd_msvc!PECMD_EvalSpecialToken+0x459
000000ea0bf6dee0 00007ff7 189b6b21 pecmd_msvc!PECMD_ProcessScriptBlock+0x1289
… DispatchExpressionBlock+0xe61 ← RunScriptText+0xb6d ← srx_ExecuteScriptFile+0x64b
… RunCommand+0x124d ← ProcessScriptBlock+0x1c75 ← DispatchExpressionBlock ← RunScriptText
… RunStartupScript+0x14cf ← RunBootScriptInFiber+0x1c ← MainW+0x5d7 ← main+0xf0
```
- **死点 RIP RVA**：捕获 RIP 在 ntdll（模块外）`RtlReAllocateHeap+0x1259`；死点环（自旋主体）RVA = **0x1127c2（+0x202 环头）/ 0x11286d（+0x2ad StrBldGrowWide 调用）**。
- **环结构反汇编摘录**（FUN_14007A224，基 RVA 0x1125c0）：
```
+0x202 (0x1127c2): mov rax,[rsp+78h]; mov [rsp+0B8h],rax; mov rax,[rsp+60h]
                   cmp [rsp+1E8h],rax; jbe 终止; mov rax,[rsp+48h]; movzx eax,word[rax]
                   test eax,eax; jne +0x2a5        ← *inP≠0 即继续（含 '%'）
+0x265 (0x112865): lea rcx,[rsp+218h]; call PECMD_StrBldGrowWide   ← 每迭代容量+0x400
+0x288 (0x112890): cmp eax,25h; je +0x380           ← '%' 转 % 处理
+0x2c2..+0x2db:   内环遇 '%' → jmp +0x37b → jmp +0x202（不推进输入游标）
+0x380 (0x112940): movzx eax,byte[rsp+400h]; and eax,1; je +0x80e  ← opt&1 门
```
  自旋回路：+0x202 → +0x2a5（grow）→ 检测 '%' → +0x37b → +0x202（inP 不动）。
- **关键内存读数**（帧 RSP 0xea0bf6d730，FUN_14007A224 布局同 021 系列）：
  - 容量槽 [rsp+8 所指] dd 0xbf6d8f0 = **0x01074800**（17,221,632 字符 ≈ 34.4 MB 缓冲）
  - 缓冲基 0x217ebd8d048；输出游标槽 0xbf6d790 → 0x217ede75068 = 基 +34.3 MB（已写至缓冲尾）
  - 输入文本区：`ENVI C=%A%_%B%`（0x217e75fc398 起）；**inP [rsp+48h]=0x217e75fc3a6（'%'），param_2 槽 [rsp+3E8h]=0x217e75fc3ac（'_'，已越过闭合 %）** —— 游标分裂签名
  - 输出缓冲 du 0x217ebd8d048 = `" C=HelloHelloHelloHelloHello…"`（%A%→"Hello" 无限重复）
  - 窗口计数 [rsp+0B8h]=1；mode [rsp+3F8h]=0、opt [rsp+400h]=1
- **寄存器快照（thread0，RtlCopyMemory 帧）**：rax=0x217e9c93040 rbx=0x217e75d0000 rcx=0x217ebb00790 **rdx=0x20fa000（≈34 MB 拷贝量）** rsi=0x217e9c93040 rdi=0x217e9c93030 r8=0x3a r9=0x9ec2 r11=0x217ede7584a r12=0x20e900a r13=0x217e75d0000 r14=0x217ebd8d030 r15=0x102 rsp=0xea0bf6d298 rbp=0xea0bf6d520
- **根因假设**：
  - **H1（强，活体+反汇编双证）**：`%`-处理分支只写 `line` 槽而不推进 `inP`，环头检查 `*inP` → inP 永停 `%` → 每迭代 StrBldGrowWide 容量 +0x400 → 缓冲 34 MB 二次方增长 → 重分配拷贝自旋。输出内容（"Hello"×N）证明闭 % 变量路径每迭代重展开同值。
  - H2（弱，症状解释）输出/容量数值本身非根因，仅为 H1 的度量面。

---

## 3. 004_envi_global_scope

- 挂死确认：18s 存活，PID **12396**（64 MB）。session: `sess-18cf6abe81045a64-3`。注意：attach 后 run_case 收尾记录的 exit=**0xC0000354**（调试器终止面缘，非 124）——该 exit.txt 被取证扰动，不作判据；挂死状态在 attach 前已确认。
- **完整 k 25（主线程；线程 1-3 线程池等待支座；无 TEAM worker 存活）**：
```
KERNELBASE!StrCmpNIW+0x26                    ← RIP（模块外）
pecmd_msvc!FUN_14007A224+0x889               ← 返回 RVA 0x122e49
pecmd_msvc!PECMD_EvalSpecialToken+0x459
pecmd_msvc!PECMD_ProcessScriptBlock+0x1289
… 其后与 002 完全同构（DispatchExpressionBlock/RunScriptText/srx/RunCommand/RunStartupScript/RunBootScriptInFiber/MainW/main）
```
- **死点 RIP RVA**：捕获 RIP 在 KERNELBASE（模块外）；返回帧 RVA **0x122e49 = FUN_14007A224+0x889**（RANDOM% 名字比较相位，src:281）；死点环同 002（0x1127c2）。
- **环结构摘录**（+0x860 区）：`+0x86f: r8=7; rdx=&"RANDOM%"; rcx=[rsp+178h]; call StrCmpNIW`；`+0x880: rdx=&"__THIS%" … call StrCmpNIW`；后接名字扫描环 +0x90d（src:283-304 同构）。
- **关键内存读数**（帧 RSP 0xdab37bd890）：名字串 0x261990bb8c8=`"A%"`；**inP [rsp+48h]=0x261990bb8c6（'%'），param_2 槽=0x261990bb8cc（""，越过闭合%）**；容量槽 dd 0xdab37bda50=**0x00ff9000**（33.5 MB）；输出基 0x2619d672048 du=`" B=innerinnerinner…"`（A="inner" 来自 `TEAM ENVI A=inner`）；mode/opt=0/1。
- **寄存器快照（thread0）**：rcx=0x261990bb8c8（名字）rdx=0x7ff718ac4790（"RANDOM%" 字面量）r8=7 rbx=0x26199090920 **r11=0x0065006e006e0069（"inne"）** r14=0x261990bb8c8 rsi=7 rdi=0x2619906a30 rsp=0xdab37bd800 rbp=0x7ff718ac4790
- **根因假设**：与 002 同（H1 强）。004 特异性：r19a §4.3 曾判"TEAM worker 停摆+主线程 join"——本轮活体**未观察到任何 TEAM worker 线程**，主线程独占自旋于展开器；TEAM 语义（线程/内联）与死点正交，需独立工单核 TEAM 实现，本死点根因与 TEAM 无关。

---

## 4. 038_sub_param_shadow

- 挂死确认：18s 存活，PID **18332**（57 MB）。复现任务收尾 exit=124、产物双缺。session: `sess-18cf6aec82923a94-4`。
- **完整 k 25（主线程；线程 1-3 等待支座）**：
```
KERNEL32+0x7cc3                                ← RIP（模块外）
KERNEL32+0x5e60
KERNELBASE!StrCmpNIW+0xdb
pecmd_msvc!FUN_14007A224+0x8da               ← 返回 RVA 0x122e9a（__THIS% 比较后）
pecmd_msvc!PECMD_EvalSpecialToken+0x459
pecmd_msvc!PECMD_ProcessScriptBlock+0x1289
… 其后与 002/004 完全同构
```
- **死点 RIP RVA**：捕获 RIP 在 KERNEL32/KERNELBASE（模块外）；返回帧 RVA **0x122e9a = FUN_14007A224+0x8da**；死点环同前。
- **关键内存读数**（帧 RSP 0x78d6cfd7f0）：**输入整行 du 0x2308ddadfd8 = `"ENVI H=%a%"`（末行）**；inP [rsp+48h]=0x2308ddadfe6（'%'），param_2 槽=0x2308ddadfec（""）；容量槽 dd 0x78d6cfd9b0=**0x00fe3000**（33 MB）；opt=1。
- **寄存器快照（thread0）**：rbx=0x78d6cfd650 rcx=0x2020744 rdx=0 rsi=1 rdi=0x2308ddadfe8 r8=0x2308dd94510 r9=0x78d6cfd6c8 r11=0x78d6cfd6c8 rsp=0x78d6cfd5a0
- **根因假设**：
  - **H1（强）**：与 002/004 同——FUN_14007A224 主环游标同步缺失，本轮触发于末行 `ENVI H=%a%`（%a%="global"）。
  - **H2（中，038 特有前置面）**：挂死发生在**末行** ⇒ `CALL F,param` 的子体 `TEAM ENVI G=%a%` **没有执行**（若执行，该行会先于 H 行触发同一自旋）——与 r19a §4.2「子体从未执行、CALL 走绕过 LAB_14004c525 的异常出口」的探针结论一致。形参遮蔽（_SUB F,a ↔ CALL F,param）在本轮挂死中不构成直接因素。

---

## 5. 对照实验（触发条件判定）

| 实验 | 脚本 | 结果 | 判定 |
|---|---|---|---|
| 003_envi_empty（语料复跑，同构建） | `ENVI A=` / `ENVI B=%A%`（A 空） | **exit=0**（00:56 与本次复跑均过） | 空值不触发 |
| T1（手工门面外实验，LOAD 同构场景） | `ENVI A=Hello` / `ENVI B=%A%`（A 非空） | **15s 存活挂死，exit=124** | 非空值触发 |
| 002/004/038 | 值均非空（Hello/inner/global） | 全部挂死 | 一致 |

> 空值 vs 非空值的分岔点：静态上 src 的闭 % 变量空/非空两路游标行为相同（皆不推进 inP），实证却非空才挂——推断**空值（或 ENVI A= ⇒ A 未定义）路径在部署二进制中存在本取证未捕获的游标推进点**（候选：not_found→env_expand 链，或 lookup 空值即 node==0 的分支尾）。列为下轮断点核实项，不影响三案死点的确定性。

---

## 6. 根因主假设（三案共享）与 dc 对照

**dc 语义（事实源）**：FUN_14007a224（dc:77689，Ghidra 标注 "Type propagation algorithm not settling"）的扫描环头
`LAB_14007a345: … pWVar8 = param_2;`（dc:77749-77752）——**游标唯一事实源 = param_2，环头每迭代重载**；全部 `%` 分支
（`%%` dc:77767-77772 `param_2 = pWVar8 + 2`；变量解析 dc:77889 `param_2 = pWVar16 + 1`；env 展开 dc:78012 等）推进的都是 param_2，
`goto LAB_14007a345` 后游标自然前移。**原版（=dc）因此不挂，002/004/038 原版 exit=0 印证。**

**src 转写缺陷（死点机制）**：src/lang/core_execline.c FUN_14007A224 把 dc 单游标拆成 `inP`（环头检查：src:170 `*inP==0`、src:181 `*inP!='%'`）与 `line`（`%` 族分支写入：src:204/209/225/232/249/302/312/331/421/447），**环头 src:168 无 `inP = line;` 复载** →
闭 % 变量展开（值非空）后 inP 停在 `'%'` 不前进 → 环头重入 `%` 处理 → 每次迭代 `PECMD_StrBldGrowWide`（src:178，容量+0x400 字符）
→ 输出 = 变量值无限重复、缓冲 33-34 MB 膨胀 → 重分配/比较相位间周而复始 → exit=124。

**同文件内已有同款修复模板（关键旁证）**：孪生函数 PECMD_ExpandEnvVars（dc FUN_14007af60，src:622-1166，"与 ExecLine2B 结构相同"）
环头 src:680 `inP = line;`、拷贝出口 src:1162
`inP = raw_src; /* H1: dc:78742 循环头 param_2=pWVar13 强制同步主游标(零长拷贝也同步) */` ——A224 主环独缺同款同步。

三线归属（死点 ID）：
- map：`0001:001115c0 FUN_14007A224 00000001401125c0 f core_execline.obj`（RVA 0x1125c0）
- src：src/lang/core_execline.c:119（头注释「完整展开（变量/参数/环境变量）」；环头 168、grow 178、`%` 分支 198-312、copy_common 354）
- dc：reference/decompiled.c:77685-77767+（签名 77689；环头 LAB_14007a345 ≈77749；`%%` 分支 77767；表达式块 `%name%` 路径 ≈77889）

---

## 7. 下一步建议（候选修复点，不落码）

1. **（最高置信，最贴 dc）A224 环头补游标复载**：src:168 `for(;;)` 首行加 `inP = line;`，形态= dc:77750 `pWVar8 = param_2`，
   可直接照抄同文件 src:680/1162 孪生模板。安全性：核过全部 continue 出口的 line 均已推进（copy 环 ✓ / `%%` `%#` `%*` `%@` ✓ / 未闭合% ✓ / var 路径 ✓ / joined '%' ✓；env_expand 尾部无 line 更新但其入口早已设 line），环头复载不会回退。
2. **（替代）分支尾同步**：在 copy_common/fmt_copy 及 `%` 族各 continue 前补 `inP = line;`（仿 src:1162）。
3. **验证计划**：修后对拍 002/004/038（期望输出 `C=Hello_World`、`B=inner`、`H=global`）+ 回归 003（保持 PASS）、021/022/023/024/025/037；同步断言输出缓冲不再膨胀。
4. **下轮核验项（空值差异）**：V1 断点选 symsnap.txt；在 FUN_14007A224 环头/`%` 分支出口设 bp 对比 003（空）与 002（非空）的 [rsp+48h](inP) 推进点；并核 `ENVI A=` 语义（定义空 vs 未定义）。
5. **038 独立工单（正交面）**：CALL F,param 子体调度缺失（r19a §4.2）——展开器修复后 038 预计不再挂死（H 将正确为 global），但 `G`（子体内 TEAM ENVI G=%a%）仍缺——需再还原 CALL 分发异常出口（dc:44304→FUN_140045c44 / src core_b3e 族），与本次死点隔离。

---

## 8. 会话与清理账目

| 案 | 复现 job | PID | session_id | end_session | taskkill | exit 记录 |
|---|---|---|---|---|---|---|
| 002 | bash-4 | 11444 | sess-18cf69fa3571daf0-2 | ✓ | ✓ | 124 / 产物双缺 |
| 004 | bash-5 | 12396 | sess-18cf6abe81045a64-3 | ✓ | ✓（超时自然收尾） | 0xC0000354（attach 扰动，非判据） |
| 038 | bash-6 | 18332 | sess-18cf6aec82923a94-4 | ✓ | ✓ | 124 / 产物双缺 |

全程无 env 残留 pecmd_msvc.exe；无在途 windbg 会话；工作树只读（未改源码/未构建/未部署/未 commit）。

---
*生成：R24 活体取证子代理 · 证据绑定构建身份 f8e7cbc/00:55:42 · windbg MCP 全程可用（无 SKIP）*